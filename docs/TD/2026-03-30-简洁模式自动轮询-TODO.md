# TODO: 简洁模式自动轮询 - 实施任务清单

> 创建日期：2026-03-30
> 基于 TD v3 (`docs/TD/2026-03-30-简洁模式自动轮询-TD.md`)
> 预计工作量：~8 文件，~450 行代码

---

## 任务概览

| 阶段 | 任务数 | 预计时间 | 状态 |
|------|-------|---------|------|
| Phase 1: 后端实现 | 2 | 30 min | ⬜ 待开始 |
| Phase 2: 前端核心 | 2 | 60 min | ⬜ 待开始 |
| Phase 3: 前端设置 | 3 | 45 min | ⬜ 待开始 |
| Phase 4: 前端样式 | 3 | 30 min | ⬜ 待开始 |
| Phase 5: 测试 | 3 | 120 min | ⬜ 待开始 |
| Phase 6: 验收 | 1 | 75 min | ⬜ 待开始 |

**说明**：Phase 4 增加到 3 个任务（i18n 单独列出），Phase 6 验收时间增加到 75 分钟（新增 3 项边界场景测试）。

---

## Phase 1: 后端实现

### Task 1.1：修改 `outlook_web/db.py` — 添加默认值

**位置**：第 759 行之后

```python
# 简洁模式自动轮询配置
cursor.execute("""
    INSERT OR IGNORE INTO settings (key, value)
    VALUES ('enable_compact_auto_poll', 'false')
    """)
cursor.execute("""
    INSERT OR IGNORE INTO settings (key, value)
    VALUES ('compact_poll_interval', '10')
    """)
cursor.execute("""
    INSERT OR IGNORE INTO settings (key, value)
    VALUES ('compact_poll_max_duration', '60')
    """)
```

**检查点**：
- [ ] 使用 `INSERT OR IGNORE`（不会覆盖已有值）
- [ ] 3 个 key 与 `settings.py` 中的 key 完全一致
- [ ] 默认值：enable=false, interval=10, max_duration=60

---

### Task 1.2：修改 `outlook_web/controllers/settings.py` — GET/PUT 扩展

#### 1.2.1 GET 返回（第 208 行之后追加）

```python
# 简洁模式自动轮询配置
"enable_compact_auto_poll": all_settings.get("enable_compact_auto_poll", "false") == "true",
"compact_poll_interval": int(all_settings.get("compact_poll_interval", "10")),
"compact_poll_max_duration": int(all_settings.get("compact_poll_max_duration", "60")),
```

#### 1.2.2 PUT 校验+保存（第 852 行之后追加）

```python
# 简洁模式自动轮询配置
if "enable_compact_auto_poll" in data:
    val = str(data["enable_compact_auto_poll"]).lower()
    if val in ("true", "false"):
        queue_setting_update("enable_compact_auto_poll", val)
        updated.append("简洁轮询开关")
    else:
        errors.append("简洁模式自动轮询开关必须是 true 或 false")

if "compact_poll_interval" in data:
    try:
        interval = int(data["compact_poll_interval"])
        if interval < 3 or interval > 60:
            errors.append("简洁模式轮询间隔必须在 3-60 秒之间")
        else:
            queue_setting_update("compact_poll_interval", str(interval))
            updated.append("简洁轮询间隔")
    except ValueError:
        errors.append("简洁模式轮询间隔必须是数字")

if "compact_poll_max_duration" in data:
    try:
        duration = int(data["compact_poll_max_duration"])
        if duration < 10 or duration > 600:
            errors.append("最大监听时长必须在 10-600 秒之间")
        else:
            queue_setting_update("compact_poll_max_duration", str(duration))
            updated.append("简洁轮询时长")
    except ValueError:
        errors.append("最大监听时长必须是数字")
```

**检查点**：
- [ ] GET 返回 bool/number 类型（非字符串）
- [ ] PUT 校验范围：interval 3-60, max_duration 10-600
- [ ] updated 消息："简洁轮询开关/间隔/时长"
- [ ] errors 消息与中文一致

---

## Phase 2: 前端核心

### Task 2.1：修改 `static/js/features/emails.js` — 派发事件

**位置**：`copyEmail()` 函数，第 952 行和第 974 行之后

在两个 `showToast('邮箱地址已复制', 'success');` 之后各插入：

```javascript
// 复制成功后派发事件，mailbox_compact.js 监听并决定是否启动轮询
window.dispatchEvent(new CustomEvent('email-copied', {
    detail: { email }
}));
```

**检查点**：
- [ ] 两处（Clipboard API 和 execCommand fallback）都插入
- [ ] CustomEvent 拼写正确
- [ ] detail 包含 email 字段

---

### Task 2.2：修改 `static/js/features/mailbox_compact.js` — 轮询引擎

**位置**：文件末尾 `window.addEventListener('ui-language-changed', ...)` 之前

#### 2.2.1 状态变量（~15 行）

```javascript
// ==================== 简洁模式自动轮询 ====================

const compactPollMap = new Map();
let compactPollEnabled = false;
let compactPollInterval = 10;
let compactPollMaxDuration = 60;
let compactPollCountdownTimer = null;
const COMPACT_POLL_TOAST_DURATION = 5000;
```

#### 2.2.2 辅助函数（~80 行）

```javascript
function getCompactAccountByEmail(email) { ... }
function findCompactAccountRow(email) { ... }  // 注意：用 .mail-row，不用 CSS.escape
function ensurePollDot(rowElement) { ... }
function updateCompactPollUI(email, state, remaining) { ... }
```

**关键修正**：
- `findCompactAccountRow` 使用 `.closest('.mail-row')`（不是 `.compact-account-row`）
- `[onclick*="${email}"]` 不使用 `CSS.escape(email)`
- 停止状态下 onclick 从 `accountsCache` 反查 accountId

#### 2.2.3 核心轮询函数（~180 行）

```javascript
async function startCompactAutoPoll(email, accountId) { ... }
async function pollSingleEmail(email) { ... }
function stopCompactAutoPoll(email, silent = false) { ... }
function stopAllCompactAutoPolls() { ... }
```

**关键修正**：
- `startCompactAutoPoll`：先 `compactPollMap.set()` + `updateCompactPollUI()` 再 baseline 拉取
- `pollSingleEmail`：Toast 传入 `COMPACT_POLL_TOAST_DURATION`（5000）
- `pollSingleEmail`：finally 中检查 `compactPollMap.has(email)` 再重置 isPolling
- `pollSingleEmail`：验证码提取区分 `!extractRes.ok`（网络错误）和无验证码

#### 2.2.4 全局倒计时（~30 行）

```javascript
function startGlobalCountdown() { ... }  // 超时邮箱批量处理
```

#### 2.2.5 局部行更新（~50 行）

```javascript
function updateSingleRowFromCache(email) { ... }  // 包含 uiMissCount 兜底
```

**关键修正**：
- snippet-meta 包含 `latest_email_folder`（与 renderCompactAccountList 一致）
- 连续 5 次 UI 更新失败时停止轮询

#### 2.2.6 全量渲染后重新应用（~15 行）

```javascript
function reapplyAllCompactPollUI() { ... }  // early return + uiMissCount 重置
```

#### 2.2.7 设置同步（~25 行）

```javascript
function applyCompactPollSettings(settings) { ... }
function applyCompactPollSettingsToRunningPolls() { ... }
```

#### 2.2.8 事件监听（~60 行）

```javascript
window.addEventListener('email-copied', function(e) { ... });
document.addEventListener('visibilitychange', function() { ... });
```

#### 2.2.9 renderCompactAccountList 集成

**位置**：`renderCompactAccountList()` 函数末尾

```javascript
// 渲染完成后，重新应用轮询状态
if (typeof reapplyAllCompactPollUI === 'function') {
    reapplyAllCompactPollUI();
}
```

**检查点**：
- [ ] 所有函数名拼写正确
- [ ] `.mail-row` 选择器（不是 `.compact-account-row`）
- [ ] 无 `CSS.escape(email)`
- [ ] 所有 Toast 传入第 4 参数 `COMPACT_POLL_TOAST_DURATION`
- [ ] finally 中有 `if (compactPollMap.has(email))` 检查
- [ ] snippet-meta 包含 folder 字段
- [ ] uiMissCount 逻辑完整
- [ ] reapplyAllCompactPollUI 有 early return
- [ ] renderCompactAccountList 末尾调用 reapplyAllCompactPollUI

---

## Phase 3: 前端设置

### Task 3.1：修改 `static/js/main.js` — showToast duration 参数

**位置**：第 1124 行 `showToast` 函数签名

**修改前**：
```javascript
function showToast(message, type = 'info', errorDetail = null) {
```

**修改后**：
```javascript
function showToast(message, type = 'info', errorDetail = null, duration = null) {
```

**修改 setTimeout 部分**：
```javascript
const autoHideMs = duration !== null ? duration : ((errorDetail && type === 'error') ? 8000 : 3000);
```

**检查点**：
- [ ] 第 4 参数默认 `null`
- [ ] `duration !== null` 时使用传入值
- [ ] 保持原有默认行为（3000/8000）

---

### Task 3.2：修改 `static/js/main.js` — 设置变量

**位置**：第 34 行，轮询变量区域之后

```javascript
// 简洁模式自动轮询设置
let compactPollEnabled = false;
let compactPollInterval = 10;
let compactPollMaxDuration = 60;
```

---

### Task 3.3：修改 `static/js/main.js` — 设置加载/保存

#### 3.3.1 设置加载（第 1821 行之后）

```javascript
// 加载简洁模式自动轮询设置
compactPollEnabled = !!data.settings.enable_compact_auto_poll;
compactPollInterval = parseIntegerSetting(data.settings.compact_poll_interval, 10);
compactPollMaxDuration = parseIntegerSetting(data.settings.compact_poll_max_duration, 60);

// 同步到 mailbox_compact.js 的全局变量
if (typeof applyCompactPollSettings === 'function') {
    applyCompactPollSettings({
        enabled: compactPollEnabled,
        interval: compactPollInterval,
        maxDuration: compactPollMaxDuration
    });
}
```

#### 3.3.2 设置保存（第 2117 行之后）

```javascript
// 简洁模式自动轮询配置
const compactPollEnabledEl = document.getElementById('enableCompactAutoPoll');
const compactPollIntervalEl = document.getElementById('compactPollInterval');
const compactPollMaxDurationEl = document.getElementById('compactPollMaxDuration');
const cEnabled = compactPollEnabledEl ? compactPollEnabledEl.checked : false;
const cInterval = compactPollIntervalEl ? parseInt(compactPollIntervalEl.value) : NaN;
const cDuration = compactPollMaxDurationEl ? parseInt(compactPollMaxDurationEl.value) : NaN;

if (isNaN(cInterval) || cInterval < 3 || cInterval > 60) {
    showToast(translateAppTextLocal('简洁模式轮询间隔必须在 3-60 秒之间'), 'error');
    return;
}
if (isNaN(cDuration) || cDuration < 10 || cDuration > 600) {
    showToast(translateAppTextLocal('最大监听时长必须在 10-600 秒之间'), 'error');
    return;
}

settings.enable_compact_auto_poll = cEnabled;
settings.compact_poll_interval = cInterval;
settings.compact_poll_max_duration = cDuration;

// 保存后立即同步到 mailbox_compact.js
if (typeof applyCompactPollSettings === 'function') {
    applyCompactPollSettings({
        enabled: cEnabled,
        interval: cInterval,
        maxDuration: cDuration
    });
}
```

**检查点**：
- [ ] 设置加载时调用 `applyCompactPollSettings`
- [ ] 设置保存后调用 `applyCompactPollSettings`
- [ ] 校验范围与后端一致
- [ ] `typeof` 守卫保护函数调用

---

## Phase 4: 前端样式

### Task 4.1：修改 `templates/index.html` — 设置面板

**位置**：第 655 行（"自动轮询设置"区块 `</div>` 之后）

```html
<!-- 简洁模式自动轮询设置 -->
<div style="border-top: 1px solid var(--border-light); margin: 1.5rem 0; padding-top: 1.5rem;">
    <h4 style="font-size: 0.95rem; font-weight: 600; margin-bottom: 1rem; color: var(--text);">📋 <span data-i18n="简洁模式自动轮询">简洁模式自动轮询</span></h4>
    <div class="form-group">
        <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
            <input type="checkbox" id="enableCompactAutoPoll">
            <span class="form-label" style="margin: 0;" data-i18n="复制邮箱后自动监听">复制邮箱后自动监听</span>
        </label>
        <div class="form-hint" data-i18n="简洁模式自动轮询说明">在简洁模式下，复制邮箱地址后自动拉取邮件直到发现新邮件并自动提取验证码</div>
        <div class="form-hint" style="color: var(--clr-warn);">
            ⚠ <span data-i18n="简洁模式轮询内存提示">轮询状态保存在浏览器内存中，刷新页面后轮询会中断，请重新复制邮箱地址以重新开始监听。修改轮询设置后会立即应用到正在进行的轮询。</span>
        </div>
    </div>
    <div class="form-group">
        <label class="form-label"><span data-i18n="轮询间隔">轮询间隔</span></label>
        <div style="display: flex; align-items: center; gap: 8px;">
            <input type="number" class="form-input" id="compactPollInterval" min="3" max="60" value="10" placeholder="10" style="max-width: 120px;">
            <span style="font-size: 0.88rem; color: var(--text-muted); font-weight: 500;" data-i18n="秒">秒</span>
        </div>
        <div class="form-hint" data-i18n="轮询间隔范围提示">范围: 3-60 秒</div>
    </div>
    <div class="form-group">
        <label class="form-label"><span data-i18n="最大监听时长">最大监听时长</span></label>
        <div style="display: flex; align-items: center; gap: 8px;">
            <input type="number" class="form-input" id="compactPollMaxDuration" min="10" max="600" value="60" placeholder="60" style="max-width: 120px;">
            <span style="font-size: 0.88rem; color: var(--text-muted); font-weight: 500;" data-i18n="秒">秒</span>
        </div>
        <div class="form-hint" data-i18n="最大监听时长范围提示">范围: 10-600 秒，到时间后自动停止监听</div>
    </div>
</div>
```

**检查点**：
- [ ] id 与 main.js 中的 `getElementById` 一致
- [ ] 所有文本有 `data-i18n` 属性
- [ ] min/max 值与后端校验一致

---

### Task 4.2：修改 `static/css/main.css` — 样式

**位置**：第 1156 行（`pulse-dot` 动画附近）

```css
/* 简洁模式自动轮询 - 脉冲圆点 */
.compact-poll-dot {
    position: absolute;
    top: 4px;
    right: 4px;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--clr-success, #28a745);
    animation: pulse-dot 1.5s ease-in-out infinite;
    pointer-events: none;
}

/* 简洁模式自动轮询 - 激活态按钮 */
.pull-button.compact-poll-active {
    border-color: var(--clr-danger, #dc3545);
    background: rgba(220, 53, 69, 0.08);
    color: var(--clr-danger, #dc3545);
    min-width: 100px;
    text-align: center;
}

.pull-button.compact-poll-active:hover {
    border-color: var(--clr-danger, #dc3545);
    background: rgba(220, 53, 69, 0.15);
}

/* 暗色模式适配 */
[data-theme="dark"] .pull-button.compact-poll-active {
    background: rgba(220, 53, 69, 0.15);
}

[data-theme="dark"] .pull-button.compact-poll-active:hover {
    background: rgba(220, 53, 69, 0.25);
}
```

**检查点**：
- [ ] min-width: 100px（不是 120px）
- [ ] 暗色模式覆盖存在
- [ ] pulse-dot 动画名称与现有定义一致

---

### Task 4.3：修改 `static/js/i18n.js` — 翻译词条

**位置**：第 220 行（现有轮询翻译之后）

```javascript
// 简洁模式自动轮询
'📋 简洁模式自动轮询': '📋 Compact Mode Auto Polling',
'复制邮箱后自动监听': 'Auto-monitor after copying email',
'简洁模式自动轮询说明': 'In compact mode, automatically fetch emails after copying an email address until a new email is detected and verification code is extracted',
'简洁模式轮询内存提示': 'Polling state is stored in browser memory. Refreshing the page will interrupt polling. Please re-copy the email address to restart monitoring. Changes take effect immediately on running polls.',
'最大监听时长': 'Max Monitoring Duration',
'到时间后自动停止监听': 'Automatically stop monitoring when time is up',
'停止监听': 'Stop Listening',
'监听超时，未检测到新邮件': 'Monitoring timeout, no new email detected',
'检测到验证码': 'Verification code detected',
'已复制到剪贴板': 'copied to clipboard',
'发现新邮件': 'New email detected',
'拉取失败，已停止监听': 'Fetch failed, monitoring stopped',
'已停止监听': 'Monitoring stopped',
'账号已被删除，已停止监听': 'Account has been deleted, monitoring stopped',
'页面元素丢失，已停止监听': 'Page element lost, monitoring stopped',
'轮询间隔范围提示': 'Range: 3-60 seconds',
'最大监听时长范围提示': 'Range: 10-600 seconds',
```

**检查点**：
- [ ] 17 个词条（含 UI 面板类）
- [ ] 中英文对应
- [ ] 与 HTML `data-i18n` 值一致

---

## Phase 5: 测试

### Task 5.1：编写 `tests/test_compact_poll_settings.py`

**参考**：`tests/TDD/2026-03-30-简洁模式自动轮询-TDD.md` §2

**用例**（12 个）：
- [ ] TC-A01: GET 返回默认值
- [ ] TC-A02: PUT + GET 回环
- [ ] TC-A03: enable 非 bool 拒绝
- [ ] TC-A04: interval 范围 3-60
- [ ] TC-A05: max_duration 范围 10-600
- [ ] TC-A06: 非数字值拒绝
- [ ] TC-A07: DB 默认值
- [ ] TC-A08: 字符串 "false"
- [ ] TC-A09: 与标准轮询隔离
- [ ] TC-A10: 部分字段
- [ ] TC-A11: updated 列表
- [ ] TC-A12: 未登录拒绝

---

### Task 5.2：编写 `tests/test_compact_poll_frontend_contract.py`

**参考**：`tests/TDD/2026-03-30-简洁模式自动轮询-TDD.md` §3

**用例**（14 个）：
- [ ] TC-B01: 设置面板元素
- [ ] TC-B02: i18n 属性
- [ ] TC-B03: JS 变量声明
- [ ] TC-B04: applyCompactPollSettings 调用
- [ ] TC-B05: email-copied 派发
- [ ] TC-B06: email-copied 监听
- [ ] TC-B07: 核心函数声明
- [ ] TC-B08: .mail-row 选择器
- [ ] TC-B09: 无 CSS.escape
- [ ] TC-B10: i18n 词条完整
- [ ] TC-B11: CSS 类名
- [ ] TC-B12: 暗色模式
- [ ] TC-B13: Toast 常量
- [ ] TC-B14: visibilitychange

---

### Task 5.3：编写 Jest 测试

**文件**：
- [ ] `tests/compact-poll/jest.config.js`
- [ ] `tests/compact-poll/setup.js`
- [ ] `tests/compact-poll/compact-poll-engine.test.js`

**用例**（22 个）：
守卫条件（5）：
- [ ] TC-C01: enabled=false 不触发
- [ ] TC-C02: enabled=true + compact 触发
- [ ] TC-C03: 非 compact 不触发
- [ ] TC-C04: tempEmailGroup 不触发
- [ ] TC-C05: 未知邮箱不触发

状态管理（4）：
- [ ] TC-C06: 同一邮箱重置
- [ ] TC-C07: 多邮箱并行
- [ ] TC-C08: stopCompactAutoPoll
- [ ] TC-C09: stopAllCompactAutoPolls

超时与错误（4）：
- [ ] TC-C10: 超时自动停止
- [ ] TC-C11: 连续 3 次失败停止
- [ ] TC-C12: 成功后 errorCount 归零
- [ ] TC-C13: 404 自动停止

防重入（1）：
- [ ] TC-C14: isPolling 锁

发现新邮件（2）：
- [ ] TC-C15: 有验证码
- [ ] TC-C16: 无验证码 fallback

设置变更（2）：
- [ ] TC-C17: enabled=false 停止所有
- [ ] TC-C18: interval 重建定时器

页面可见性（2）：
- [ ] TC-C19: hidden 暂停
- [ ] TC-C20: visible 恢复

DOM 与 UI（2）：
- [ ] TC-C21: findCompactAccountRow
- [ ] TC-C22: updateCompactPollUI

---

## Phase 6: 验收

### Task 6.1：手动测试验收

**参考**：TD §6.1 + PRD §6

**检查项**（15 项）：

#### 基本功能（12 项）
- [ ] 基本功能：开启 → 复制 → 立即显示指示器 → 新邮件 → 自动提取验证码
- [ ] 并行轮询：3 个邮箱独立倒计时
- [ ] 重复触发：同一邮箱重置
- [ ] 手动停止：点击"停止监听" → 恢复"拉取" → 手动拉取正常
- [ ] 超时停止：10 秒后 Toast（5 秒消失）
- [ ] 页面切换：切后台 → 切回 → 立即检查
- [ ] 页面刷新：轮询全部中断
- [ ] 临时邮箱：不触发轮询
- [ ] 设置即时生效：轮询中修改间隔 → 定时器重建
- [ ] 暗色模式：激活态按钮可见
- [ ] Toast 5 秒：确认 5 秒后消失
- [ ] UI 元素丢失兜底：全量渲染后恢复指示器

#### 边界场景（3 项）
- [ ] 切换标准模式：简洁模式轮询中 → 切换到标准模式 → 轮询在后台继续 → 切回简洁模式指示器恢复
- [ ] 切换分组：轮询中 → 切换到其他分组 → 轮询不受影响继续运行 → 切回原分组指示器恢复
- [ ] 多邮箱同时发现新邮件：3 个邮箱同时收到邮件 → 各自独立处理 → Toast 5 秒后不堆叠

---

## 运行测试

```bash
# Python 测试（A + B 类）
python -m unittest tests.test_compact_poll_settings -v
python -m unittest tests.test_compact_poll_frontend_contract -v

# 全部 Python 测试（无回归）
python -m unittest discover -s tests -v

# Jest 测试（C 类）
cd tests/compact-poll
npx jest --config jest.config.js

# 带覆盖率
npx jest --config jest.config.js --coverage
```

---

## 完成标准

- [ ] 所有 Phase 1-4 任务完成
- [ ] 所有 48 个测试用例通过（12 + 14 + 22）
- [ ] 现有测试无回归（`python -m unittest discover -s tests -v`）
- [ ] 所有 15 项手动测试验收通过（12 项基本功能 + 3 项边界场景）

---

## 文件清单

| 文件 | 状态 | 行数 | 完成日期 |
|------|------|------|---------|
| `outlook_web/db.py` | ⬜ | ~10 | __/__/____ |
| `outlook_web/controllers/settings.py` | ⬜ | ~40 | __/__/____ |
| `static/js/features/emails.js` | ⬜ | ~3 | __/__/____ |
| `static/js/main.js` | ⬜ | ~30 | __/__/____ |
| `static/js/features/mailbox_compact.js` | ⬜ | ~250 | __/__/____ |
| `templates/index.html` | ⬜ | ~30 | __/__/____ |
| `static/js/i18n.js` | ⬜ | ~20 | __/__/____ |
| `static/css/main.css` | ⬜ | ~25 | __/__/____ |
| `tests/test_compact_poll_settings.py` | ⬜ | ~150 | __/__/____ |
| `tests/test_compact_poll_frontend_contract.py` | ⬜ | ~120 | __/__/____ |
| `tests/compact-poll/jest.config.js` | ⬜ | ~20 | __/__/____ |
| `tests/compact-poll/setup.js` | ⬜ | ~80 | __/__/____ |
| `tests/compact-poll/compact-poll-engine.test.js` | ⬜ | ~600 | __/__/____ |

---

## 注意事项

1. **关键修正点**（从外部 AI 审查）：
   - `.mail-row` 选择器（不是 `.compact-account-row`）
   - 不使用 `CSS.escape(email)`
   - baseline 拉取前先更新 UI
   - finally 中检查 `compactPollMap.has(email)`
   - snippet-meta 包含 folder 字段
   - uiMissCount 兜底逻辑
   - min-width: 100px（不是 120px）
   - 暗色模式 CSS 覆盖

2. **集成点**：
   - `renderCompactAccountList()` 末尾必须调用 `reapplyAllCompactPollUI()`
   - 设置加载/保存后必须调用 `applyCompactPollSettings()`
   - `showToast` 新增第 4 参数 `duration`

3. **测试策略**：
   - 先运行现有测试确保无回归
   - 再运行新增测试验证功能
   - 最后手动测试验证用户体验
