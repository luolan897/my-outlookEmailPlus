# TODO: 邮箱池项目维度成功复用

> 创建日期：2026-04-16
> 基于 PRD v1.0：`docs/PRD/2026-04-16-邮箱池项目维度成功复用PRD.md`
> 基于 FD v1.0：`docs/FD/2026-04-16-邮箱池项目维度成功复用FD.md`
> 基于 TD v1.0：`docs/TD/2026-04-16-邮箱池项目维度成功复用TD.md`
> 基于 TDD v1.0：`docs/TDD/2026-04-16-邮箱池项目维度成功复用TDD.md`
> 目标版本：待定

> 会话约束（必须保持）：
> 1. 默认不测试 / 不运行 / 不编译
> 2. 每次实际推进都要同步更新 `WORKSPACE.md`
> 3. 对用户的结果反馈统一通过 `寸止` MCP，不只落文档
> 4. 如果必须启动进程，只能使用新进程后台启动（如 `Start-Process` / 独立进程），不能再用前台长命令占住执行链路

---

## 任务概览

| 阶段 | 任务数 | 状态 |
|---|---:|---|
| Phase 0: 文档口径冻结与引用闭环 | 4 | ✅ 已完成 |
| Phase 1: TDD 下沉与测试代码先行落地 | 6 | ✅ 已完成编写与执行 |
| Phase 2: Schema v22 落地 | 4 | ✅ 已完成实现与验证 |
| Phase 3: Repository 状态机改造 | 6 | ✅ 已完成实现与验证 |
| Phase 4: Service / Controller 接入 | 5 | ✅ 已完成实现与验证 |
| Phase 5: 实现收敛与回归 | 5 | ✅ 已完成 |

### 当前实际状态（2026-04-16）

1. 文档层已经具备 **PRD / FD / TD / TDD / TODO** 五层闭环。
2. 测试层已经真实落地到四个文件：
   - `tests/test_db_schema_v22_pool_project_reuse.py`
   - `tests/test_pool_repository_project_reuse.py`
   - `tests/test_pool_service_project_reuse.py`
   - `tests/test_pool_flow_suite.py`（持续增量改造）
3. 业务实现层已完成 Schema / Repository / Service 主链路落地，外部接口契约保持不变。
4. 已执行迁移 / Repository / Service / Flow suite 与相关旧回归测试，共 `78` 个用例通过；并已额外执行全量 `python -m unittest discover -v`，结果为 `Ran 1187 tests`、`OK (skipped=7)`。

---

## Phase 0: 文档口径冻结与引用闭环

> 目标：把“需求是什么、边界是什么、现在做到哪了”先写清楚，避免后续实现跑偏。

### Task 0.1：冻结真实问题定义

- [x] 结论明确为：当前系统并非“完全不支持多项目”，而是 **success 后会进入全局 `used`，导致后续项目失去继续复用机会**
- [x] 在 PRD 中明确“当前已支持不同 `project_key` 间再次领取，但 success 后生命周期不完整”
- [x] 将该问题定义为独立需求，而不是文档误读

### Task 0.2：冻结第一阶段产品边界

- [x] 第一阶段仅覆盖长期邮箱
- [x] `project_key` 继续由调用方传入，不新增平台项目管理
- [x] `caller_id` / `task_id` 继续保留
- [x] 未传 `project_key` 时继续走旧路径
- [x] `cloudflare_temp_mail` / `account_type='temp_mail'` 继续排除在第一阶段之外

### Task 0.3：完成 PRD / FD / TD / TDD 联调收口

- [x] 区分“稳定态 success 防重”与“历史 `used` 迁移回池的一次性例外”
- [x] 统一“覆盖路径 / 旧路径 / 第一阶段排除的临时邮箱”术语
- [x] 将 TDD 下沉到函数级 case 清单

### Task 0.4：补齐 TODO 层引用闭环

- [x] 新建 `docs/TODO/2026-04-16-邮箱池项目维度成功复用TODO.md`
- [x] 回填 PRD / FD / TD / TDD 的 `关联 TODO`
- [x] 在 TODO 中明确“已写测试 / 未做实现 / 未执行回归”的真实状态

---

## Phase 1: TDD 下沉与测试代码先行落地

> 目标：先把目标语义锁进测试，避免实现时又回到“只改一处 status”的窄修复。

### Task 1.1：迁移层测试落地

**文件**：`tests/test_db_schema_v22_pool_project_reuse.py`

- [x] 新增 v22 字段存在性测试
- [x] 新增历史长期邮箱 `used -> available` 迁移测试
- [x] 新增 `cloudflare_temp_mail` 不误入长期邮箱迁移语义测试
- [x] 新增旧 claim 痕迹不被伪回填为 success 的测试
- [x] 新增迁移后可再次被 claim / 原项目可再拿一次的测试

### Task 1.2：Repository 层测试落地

**文件**：`tests/test_pool_repository_project_reuse.py`

- [x] `claim_atomic()` 写入 `claimed_project_key`
- [x] 同项目只有 success 记录才阻断
- [x] 不同项目 success 历史仍可 claim
- [x] reuse 路径 success 后回 `available`
- [x] `release()` / `expire_stale_claims()` 清理 claim 上下文但不制造 success
- [x] `get_stats()` 按真实主状态聚合

### Task 1.3：Service 层测试落地

**文件**：`tests/test_pool_service_project_reuse.py`

- [x] `claimed_project_key` 驱动 reuse 路径
- [x] 缺失 `claimed_project_key` 时回退旧语义
- [x] `cloudflare_temp_mail` / 临时邮箱继续走旧路径
- [x] 空白 `project_key` 不误进新语义
- [x] `invalid_result` / `token_mismatch` 校验保持不变

### Task 1.4：接口主流程测试落地

**文件**：`tests/test_pool_flow_suite.py`

- [x] 未传 `project_key` 时 success 仍为 `used`
- [x] 长期邮箱 + `project_key` + success 后返回 `available`
- [x] 同项目 success 后再次 claim 被阻断
- [x] 不同项目 success 后立即复用
- [x] `verification_timeout` / `claim-release` 后同项目可重试
- [x] stats 断言随新语义收敛
- [x] `cloudflare_temp_mail` 继续保持旧语义

### Task 1.5：测试基建前置兼容

- [x] 在相关测试 `setUp()` 中预留未来 v22 字段，避免实现接入前先被缺列卡住
- [x] 增强 pool 相关表清理，降低跨 case 污染

### Task 1.6：自动化执行保留项

- [x] 运行新增测试文件
- [x] 运行相关回归测试
- [x] 按失败结果继续收敛实现

> 备注：本阶段当前已经完成“测试代码编写 + 自动化执行 + 失败收敛”；当前保留项只剩会话收尾确认。

---

## Phase 2: Schema v22 落地

> 目标：补齐成功复用所需的数据结构与历史数据迁移能力。

### Task 2.1：升级 `outlook_web/db.py`

- [x] `DB_SCHEMA_VERSION` 升级到 v22
- [x] `accounts` 新增 `claimed_project_key`
- [x] `account_project_usage` 新增 `first_success_at / last_success_at / success_count`
- [x] 保持迁移幂等

### Task 2.2：历史数据迁移

- [x] 历史长期邮箱 `pool_status='used'` 迁回 `available`
- [x] `cloudflare_temp_mail` / `account_type='temp_mail'` 不走该迁移语义
- [x] 不伪精确回填历史 success 记录

### Task 2.3：迁移边界保持一致

- [x] 保留历史 `first_claimed_at / last_claimed_at`
- [x] 不删旧列、不改旧列名
- [x] 不制造“看起来很准但其实不准”的历史 success 数据

### Task 2.4：与测试对齐

- [x] Schema / 迁移实现与 `tests/test_db_schema_v22_pool_project_reuse.py` 对齐

---

## Phase 3: Repository 状态机改造

> 目标：让底层状态机真正具备“同项目防 success、跨项目立即复用”的语义。

### Task 3.1：改造 `claim_atomic()`

- [x] 过滤逻辑只拦截同项目 success 记录
- [x] 领取成功后写 `accounts.claimed_project_key`
- [x] 继续保留 claim log 和 claim 痕迹写入

### Task 3.2：改造 `complete()`

- [x] 覆盖路径 + `success` 时返回并落库为 `available`
- [x] upsert 项目维度 success 记录
- [x] 清空 claim 上下文（含 `claimed_project_key`）
- [x] 旧路径继续沿用现有结果映射

### Task 3.3：改造 `release()`

- [x] 清空 claim 上下文
- [x] 主状态恢复为 `available`
- [x] 不再删除 `account_project_usage` 行

### Task 3.4：改造 `expire_stale_claims()`

- [x] 过期时清空 `claimed_project_key`
- [x] 不创建 success 记录
- [x] 不删除 usage 行

### Task 3.5：保持 `get_stats()` 语义稳定

- [x] 继续按 `accounts.pool_status` 聚合
- [x] reuse 路径 success 后自然计入 `available`

### Task 3.6：与 Repository 测试对齐

- [x] 与 `tests/test_pool_repository_project_reuse.py` 对齐收敛

---

## Phase 4: Service / Controller 接入

> 目标：在不破坏现有对外契约的前提下，把 Repository 新语义接入主流程。

### Task 4.1：Service `claim_random()`

- [x] 保持现有入参与校验方式
- [x] 在覆盖路径下正确驱动 Repository 写入 `claimed_project_key`

### Task 4.2：Service `complete_claim()`

- [x] 读取 `provider / account_type / claimed_project_key`
- [x] 判断是否进入长期邮箱 reuse 路径
- [x] 把 `enable_project_reuse` 与 `claimed_project_key` 传给 Repository

### Task 4.3：旧路径兼容

- [x] 未传 `project_key` 时继续旧行为
- [x] `cloudflare_temp_mail` / 临时邮箱继续旧行为
- [x] 保持现有错误校验与错误码

### Task 4.4：Controller 契约保持稳定

- [x] `/api/external/pool/claim-complete` 请求结构不新增字段
- [x] 响应结构继续返回 `{account_id, pool_status}`
- [x] `/api/external/pool/stats` 不新增专门项目统计接口

### Task 4.5：与上层测试对齐

- [x] 与 `tests/test_pool_service_project_reuse.py`
- [x] 与 `tests/test_pool_flow_suite.py`

---

## Phase 5: 实现收敛与回归

> 目标：在实现完成后，用既有测试与文档把行为收口。

### Task 5.1：按 TDD 收敛目标语义

- [x] 新语义主路径通过
- [x] 旧语义兼容路径通过
- [x] 历史迁移路径通过

### Task 5.2：执行自动化验证

- [x] 运行迁移 / Repository / Service / Flow suite 测试
- [x] 必要时执行相关回归测试
- [x] 额外执行全量 `python -m unittest discover -v`

### Task 5.3：文档回填

- [x] TDD 更新为“已从编写进入收敛”
- [x] TODO 更新阶段状态
- [x] `WORKSPACE.md` 继续记录实现推进

### Task 5.4：契约复核

- [x] 不引入新对外字段
- [x] 不引入新错误码
- [x] stats 语义与主状态一致

### Task 5.5：会话收尾前检查

- [x] 确认文档与代码状态一致
- [x] 确认用户反馈已通过 `寸止` MCP 输出
