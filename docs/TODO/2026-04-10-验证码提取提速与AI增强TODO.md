# TODO: 验证码提取提速与 AI 增强（V1）

> 创建日期：2026-04-10  
> 关联 PRD：`docs/PRD/2026-04-10-验证码提取提速与AI增强PRD.md`  
> 关联 FD：`docs/FD/2026-04-10-验证码提取提速与AI增强FD.md`  
> 关联 TD：`docs/TD/2026-04-10-验证码提取提速与AI增强TD.md`  
> 关联 TDD：`docs/TDD/2026-04-10-验证码提取提速与AI增强-TDD.md`  
> 目标版本：v1.14.x  
> 范围基线：Web + External 同级 P0；默认 6 位仅作用于验证码；分组内 regex > length；总优先级 request > group > default。

> 需求澄清（2026-04-10 晚）：分组策略仅保留验证码长度与正则；AI 配置改为系统级（设置页统一配置 URL/API Key/模型/开关）。

> 状态修订（2026-04-11）：已按“系统级 AI + 规则优先 + AI 回退”落地；
> 新增 AI 探测接口（`/api/settings/verification-ai-test`）、模型联调脚本与解析放宽策略（以 code/link 可提取为准）。

---

## 任务概览

| 阶段 | 任务数 | 预计耗时 | 状态 |
|---|---:|---:|---|
| Phase 0: 基线冻结（文档/测试） | 4 | 30 min | ✅ 已完成 |
| Phase 1: DB Schema 与迁移 | 5 | 60 min | ✅ 已完成 |
| Phase 2: Groups Repository/API 扩展 | 8 | 120 min | ✅ 已完成 |
| Phase 3: 分组设置前端契约落地 | 6 | 90 min | ✅ 已完成 |
| Phase 4: Web 提取链路策略注入 | 7 | 120 min | ✅ 已完成 |
| Phase 5: External 提取链路对齐 | 6 | 100 min | ✅ 已完成 |
| Phase 6: 错误码与 AI 约束统一 | 5 | 60 min | ✅ 已完成 |
| Phase 7: TDD 转绿（分层） | 7 | 150 min | ✅ 已完成 |
| Phase 8: 回归与发布收尾 | 6 | 90 min | ✅ 已完成 |
| Phase 9: AI 配置上收系统级 | 6 | 80 min | ✅ 已完成 |
| Phase 10: AI 探测与模型联调 | 6 | 120 min | ✅ 已完成 |
| Phase 11: 解析策略放宽与E2E复验 | 5 | 90 min | ✅ 已完成 |

---

## Phase 0: 基线冻结（文档/测试）

### Task 0.1 文档口径最终冻结
- [x] 确认 PRD/FD/TD/TDD 四份文档口径完全一致（默认边界、优先级、AI 必填约束）。
- [x] 如有冲突，仅在文档层修正，不提前改实现代码。

### Task 0.2 测试基线确认（RED）
- [x] 新增测试文件已就位：
  - `tests/test_group_verification_policy_repo.py`
  - `tests/test_groups_verification_policy_api.py`
  - `tests/test_extract_verification_group_policy.py`
  - `tests/test_external_verification_group_policy.py`
  - `tests/test_group_policy_frontend_contract.py`
- [x] 记录当前 RED 原因清单（字段缺失/API 未接入/前端契约未落地）。
  - RED 根因（已闭环）：`groups` 缺策略字段、groups API 未透传/校验、Web/External 未注入分组策略、前端弹窗缺字段与校验。

### Task 0.3 变更边界声明
- [x] 明确本需求不处理与本主题无关的存量改动（避免误提交）。

### Task 0.4 实施顺序锁定
- [x] 锁定执行顺序：DB → Repo/API → Frontend → Web/External → Test Green。

---

## Phase 1: DB Schema 与迁移

### Task 1.1 groups 表字段扩展
- [x] 在 `outlook_web/db.py` 的 groups 建表 SQL 中新增 4 字段：
  - `verification_code_length`
  - `verification_code_regex`
  - `verification_ai_enabled`
  - `verification_ai_model`

### Task 1.2 旧库迁移幂等
- [x] 在 `PRAGMA table_info(groups)` 迁移段按字段存在性补齐 `ALTER TABLE`。
- [x] 空值回填默认值：`6-6` / `''` / `0` / `''`。

### Task 1.3 schema 版本推进
- [x] 评估并更新 `DB_SCHEMA_VERSION`（建议 +1）。
- [x] 保持升级日志与诊断接口可见性。

### Task 1.4 启动安全验证
- [x] 验证重复启动无异常（幂等通过）。

### Task 1.5 合约检查
- [x] `PRAGMA table_info(groups)` 结果包含 4 新字段。

---

## Phase 2: Groups Repository/API 扩展

### Task 2.1 Repository 参数扩展
- [x] 扩展 `outlook_web/repositories/groups.py`：`add_group()` 与 `update_group()` 接受 4 个策略参数。

### Task 2.2 Repository 校验函数
- [x] 增加策略标准化/校验函数：
  - 长度范围格式校验（`min-max`）
  - regex 可编译校验
  - AI enabled 规范化（0/1）
  - model ID 规范化（trim）

### Task 2.3 AI 必填约束
- [x] 历史 group AI 字段进入软兼容模式（忽略传入值并归零存储），不再承担 AI 必填校验。

### Task 2.4 groups Controller 入参扩展
- [x] `outlook_web/controllers/groups.py` 的 POST/PUT 接口解析并透传 4 字段。

### Task 2.5 groups Controller 错误映射
- [x] 映射并返回以下错误码（400）：
  - `GROUP_VERIFICATION_LENGTH_INVALID`
  - `GROUP_VERIFICATION_REGEX_INVALID`
  - （修订）不再新增/触发 `GROUP_AI_MODEL_REQUIRED`，AI 校验迁移到 settings。

### Task 2.6 groups 查询返回扩展
- [x] `GET /api/groups` 与 `GET /api/groups/{id}` 返回新字段。

### Task 2.7 审计字段脱敏
- [x] 审计日志仅记录“是否配置 regex/model”，不直接记录敏感内容。

### Task 2.8 回归
- [x] 原有分组 CRUD 能力（name/desc/color/proxy）保持可用。

---

## Phase 3: 分组设置前端契约落地

### Task 3.1 modal 字段补齐
- [x] 在 `templates/partials/modals.html` 保留/落地规则控件 ID：
  - `groupVerificationCodeLength`
  - `groupVerificationCodeRegex`
  - （修订）分组侧 AI 控件已移除，避免配置口径分叉。

### Task 3.2 编辑回填
- [x] `static/js/features/groups.js` 编辑分组时回填规则 2 字段（length/regex）。

### Task 3.3 保存 payload
- [x] groups 保存请求体带出：
  - `verification_code_length`
  - `verification_code_regex`
  - （修订）不再发送分组级 AI 字段。

### Task 3.4 前端即时校验
- [x] groups 前端仅做 length/regex 校验；AI 必填校验迁移到 settings 保存逻辑。

### Task 3.5 文案提示
- [x] 在 UI 中提示“同组内 regex 优先于 length”。

### Task 3.6 回归
- [x] 分组弹窗原有行为不退化（颜色、代理、名称校验）。

---

## Phase 4: Web 提取链路策略注入

### Task 4.1 策略解析器
- [x] 在 Web 提取入口引入统一策略解析步骤（可复用到 External）。

### Task 4.2 默认规则应用
- [x] 无请求参数且无分组配置时，验证码默认 `6-6`。

### Task 4.3 分组策略应用
- [x] 有分组配置时，按分组策略注入 `code_length/code_regex`。

### Task 4.4 请求覆盖分组
- [x] 请求显式参数覆盖分组策略。

### Task 4.5 组内冲突优先级
- [x] 分组同时存在 length + regex 时，优先 regex。

### Task 4.6 链接边界
- [x] 验证链接提取不受默认 6 位策略影响。

### Task 4.7 AI 必填兜底
- [x] 若系统级 AI 开启但 Base URL/API Key/模型缺失，返回 `VERIFICATION_AI_CONFIG_INCOMPLETE`。

---

## Phase 5: External 提取链路对齐

### Task 5.1 External 策略解析复用
- [x] `api_external_get_verification_code` 复用同口径策略解析。

### Task 5.2 请求参数兼容
- [x] 保持现有 query 参数能力（`code_length/code_regex/code_source`）并支持覆盖分组。

### Task 5.3 分组策略注入
- [x] 无请求参数时使用分组策略；无分组策略回退默认。

### Task 5.4 组内 regex 优先
- [x] External 行为与 Web 一致（regex > length）。

### Task 5.5 AI 必填错误
- [x] 返回稳定错误码 `VERIFICATION_AI_CONFIG_INCOMPLETE`（HTTP 400）。

### Task 5.6 回归不破坏
- [x] 保持外部既有错误语义：`VERIFICATION_CODE_NOT_FOUND` 等不被意外替换。

---

## Phase 6: 错误码与 AI 约束统一

### Task 6.1 错误码字典补全
- [x] 在 `outlook_web/errors.py` 补充中英文映射：
  - `GROUP_VERIFICATION_LENGTH_INVALID`
  - `GROUP_VERIFICATION_REGEX_INVALID`
  - `VERIFICATION_AI_CONFIG_INCOMPLETE`

### Task 6.2 错误结构统一
- [x] Web 路径与 External 路径都输出统一错误结构（含 code/message/status/trace）。

### Task 6.3 AI 模型 ID 规范
- [x] 模型 ID 在 settings 侧做非空约束；供应商/模型可通过探测接口联调筛选。

### Task 6.4 安全与日志
- [x] 禁止在日志明文打印模型配置详情。

### Task 6.5 人机提示一致
- [x] 前端提示文案与后端错误文案保持一致。

### Task 6.6 旧错误码去运行期化
- [x] `GROUP_AI_MODEL_REQUIRED` 仅保留字典兼容，不再作为运行期主错误码。

---

## Phase 7: TDD 转绿（分层）

### Task 7.1 Repo 测试转绿
- [x] `python -m unittest tests.test_group_verification_policy_repo -v`

### Task 7.2 groups API 测试转绿
- [x] `python -m unittest tests.test_groups_verification_policy_api -v`

### Task 7.3 Web 提取测试转绿
- [x] `python -m unittest tests.test_extract_verification_group_policy -v`

### Task 7.4 External 提取测试转绿
- [x] `python -m unittest tests.test_external_verification_group_policy -v`

### Task 7.5 前端契约测试转绿
- [x] `python -m unittest tests.test_group_policy_frontend_contract -v`

### Task 7.6 既有回归测试
- [x] 重点回归：
  - `tests/test_external_api.py`
  - `tests/test_verification_extractor_options.py`
  - `tests/test_core_features.py`

### Task 7.7 全量通过
- [x] `python -m unittest discover -s tests -v` 无 FAIL/ERROR。
  - 结果：`Ran 938 tests ... OK (skipped=7)`。

---

## Phase 8: 回归与发布收尾

### Task 8.1 文档回填
- [x] 回填 FD/TD/TDD 的“实现结果与偏差”。

### Task 8.2 WORKSPACE 记录
- [x] 在 `WORKSPACE.md` 记录 TODO 执行结果与关键决策。

### Task 8.3 变更说明
- [x] 更新 `CHANGELOG.md`（中文描述，突出 why）。

### Task 8.4 兼容性确认
- [x] 确认未配置策略的旧分组行为可用（默认值生效）。

### Task 8.5 风险回顾
- [x] 检查是否引入新慢点（尤其是提取前策略解析与分组查询路径）。

### Task 8.6 发布门槛
- [x] 满足“关键矩阵通过 + 全量测试通过 + 文档回填完成”后再进入发布流程。

---

## 附：本 TODO 的验收口径

1. **实现层面**：分组策略字段完成端到端贯通（DB/Repo/API/UI/Web/External）。
2. **规则层面**：优先级严格符合 PRD（request > group > default；group 内 regex > length）。
3. **边界层面**：默认 6 位仅作用于验证码，链接提取行为保持独立。
4. **约束层面**：AI 开启必须有模型 ID，错误反馈明确且一致。
5. **质量层面**：TDD 新增测试与关键回归均通过。

---

## 需求澄清后的增量任务（2026-04-10 晚）

### Phase 9: AI 配置上收系统级（已完成）

#### Task 9.1 文档口径修正
- [x] PRD/FD/TD/TDD 明确“分组仅 length/regex，AI 配置系统级”。

#### Task 9.2 groups 侧去 AI 化
- [x] groups API/Repo/UI 去除分组级 AI 字段读写与校验。
- [x] 保留兼容迁移说明（旧字段只读或忽略，避免硬断）。

#### Task 9.3 settings 增加 AI 基础配置
- [x] 设置页新增 AI 开关、Base URL、API Key、模型 ID。
- [x] API Key 采用加密存储与脱敏回显。

#### Task 9.4 提取链路改读系统级 AI 配置
- [x] Web/External 提取统一从 settings 读取 AI 配置。
- [x] 缺 URL/API Key/模型 ID 时返回统一配置错误（不静默降级）。

#### Task 9.5 长度格式易用性修复
- [x] `verification_code_length` 增加容错规范化（如 `6`→`6-6`、`4~8`→`4-8`）。
- [x] 保持错误码 `GROUP_VERIFICATION_LENGTH_INVALID` 不变。

#### Task 9.6 回归与验收
- [x] 新增/调整测试：长度容错（repo/api）与既有提取链路回归通过。
- [x] 全量测试通过后关闭 Phase 9。

补充结果：全量已通过 `python -m unittest discover -s tests -v`，结果为 `Ran 946 tests in 206.872s`，`OK (skipped=7)`。

---

## Phase 10: AI 探测与模型联调（2026-04-11，已完成）

#### Task 10.1 新增配置探测接口
- [x] 新增 `POST /api/settings/verification-ai-test`，基于已保存配置做连通性/可用性探测。

#### Task 10.2 前端探测入口
- [x] 设置页基础 Tab 新增“🤖 测试 AI 配置”按钮与结果区。

#### Task 10.3 探测口径调整
- [x] 探测口径改为连通性优先（HTTP 2xx 视为可连通），并分离 `connectivity_ok/contract_ok`。

#### Task 10.4 模型列表与候选联调
- [x] 新增脚本 `tests/verify_openai_compatible_response_format.py` 与 `tests/verify_verification_ai_endpoints.py`。
- [x] 完成 Qwen 候选模型批量探测与可用性分层。

#### Task 10.5 默认模型稳定化
- [x] 默认模型调整为已验证可用：`Qwen/Qwen3-235B-A22B-Instruct-2507`。

#### Task 10.6 文档回填
- [x] 回填 `WORKSPACE.md` 与 `docs/DEV/2026-04-11-AI系统级配置与提取链路人工测试指南.md`。

---

## Phase 11: 解析策略放宽与 E2E 复验（2026-04-11，已完成）

#### Task 11.1 AI 输出解析放宽
- [x] 解析策略改为“只要可提取到 `verification_code` 或 `verification_link` 任一即接受”。

#### Task 11.2 置信度归一化
- [x] 支持数值/布尔/字符串 confidence，统一映射为 `high/low`。

#### Task 11.3 回退策略保持
- [x] `choices=null`、429、超时等场景继续快速回退规则结果，不阻塞主链路。

#### Task 11.4 自动化验证
- [x] 相关探测与契约测试复跑通过（含 `test_settings_verification_ai_probe`）。

#### Task 11.5 端到端复验
- [x] 端到端抽样验证通过，并观测到 `ai_used=true` 的真实样本。

---

## 人工联调结果补记（2026-04-11）

1. 人工确认项（通过）：
   - 系统级 AI 完整配置可保存。
   - 分组字段范围与长度容错行为符合预期。
   - Web 验证码提取可用。
   - 自动调度心跳可观测（`/api/scheduler/status`）。
2. 人工未覆盖项（由自动化补强）：
   - AI 异常快速回退。
   - External 链路一致性。
3. 自动化补强已执行：
   - `python -m unittest tests.test_settings_verification_ai_config tests.test_verification_ai_json_contract tests.test_external_verification_group_policy -v`
   - 结果：`Ran 8 tests ... OK`。

---

## 运行态回归补记（2026-04-11 晚）

1. 针对上一轮全量中 2 个失败用例（均为 `/api/settings/email-test` 502）完成根因排查：
   - 端口 `587` 场景下环境残留 `EMAIL_NOTIFICATION_SMTP_USE_SSL=true`，触发 `SMTP_SSL` 路径冲突。
2. 已在 `outlook_web/services/email_push.py` 增加 SMTP 传输模式规范化：
   - 587 强制 STARTTLS（`use_tls=true,use_ssl=false`）
   - 465 强制 SSL（`use_ssl=true,use_tls=false`）
   - 非标准端口双开冲突时优先 SSL
3. 新增测试：`tests/test_email_push_transport_mode.py`（3 用例）验证上述规范化行为。
4. 回归结果：
   - `tests.test_notification_dispatch` + `tests.test_v190_i18n_email_notification_tdd`：`Ran 44 tests ... OK`
   - 全量：`python -m unittest discover -s tests -v` → `Ran 952 tests in 309.437s`，`OK (skipped=7)`。

---

## 接口语义修订补记（2026-04-11 夜）

1. **Web 提取接口互斥语义落地**：
   - `/api/emails/<email>/extract-verification`：有 code 时不返回 link（`verification_link=None`）。
2. **External 注册池双接口“各取各的”落地**：
   - `/api/external/verification-code`：仅返回 `verification_code`。
   - `/api/external/verification-link`：仅返回 `verification_link`。
   - 通过结果整形确保不再在单接口响应中混杂 code/link。
3. **验证结果**：
   - 定向回归 + 全量回归通过：`Ran 984 tests in 186.955s`，`OK (skipped=7)`。
4. **运行态注意事项**：
   - 若本地同时存在多个 `web_outlook_app.py` 监听 `:5000`，可能出现“命中旧实例导致行为与代码不一致”；
     需先清理旧进程再做人工验收。

5. **容器化验证补记（本次会话）**：
   - 本地镜像构建通过：`docker build -t outlook-email-plus:local-20260411 .`
   - 容器首跑出现 `sqlite3 disk I/O error`，原因为宿主机进程占用同一挂载数据库；
     停止宿主机进程后重启容器恢复正常。
   - 容器实测（5002 端口）结果与语义一致：
     - `extract-verification` 返回 `code=1181, link=None, formatted=1181`。

6. **本轮全量回归补记（2026-04-11）**：
   - 按用户“提交前全量回归”要求执行：
     - `python -m unittest discover -s tests -v`
     - 结果：`Ran 984 tests in 196.747s`，`OK (skipped=7)`。
   - 结论：当前“Web 互斥 + External 字段隔离”实现在现有测试矩阵下持续稳定，可进入本地提交阶段。
