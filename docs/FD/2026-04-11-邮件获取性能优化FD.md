# FD: 邮件获取性能优化（V1）

- 文档版本: v1.2
- 创建日期: 2026-04-11
- 最近更新: 2026-04-12（补充 A/B 实测数据与当前达标状态）
- 关联 PRD: `docs/PRD/2026-04-11-邮件获取性能优化PRD.md`（v1.3）
- 关联 TD: `docs/TD/2026-04-11-邮件获取性能优化TD.md`（v1.0）
- 关联 TDD: `docs/TDD/2026-04-11-邮件获取性能优化-TDD.md`（v1.1）
- 当前范围: 功能设计（系统行为、模块职责、接口契约、数据流），不展开代码实现细节

---

## 1. 功能定义

V1 目标：通过路径统一重构 + 6 项性能优化，将 Outlook 验证码提取端到端耗时从 8.5-15.5s 降至 ≤4s。

### 1.1 路径统一重构（前置）

将 Web 端与外部 API 的 Outlook 验证码提取路径合并为一套统一的 service 层实现。

### 1.2 性能优化（6 项）

1. **Graph API 权限预检** — 跳过无权限的 Graph 调用
2. **IMAP 连接复用** — fetch+detail 共用一次 IMAP 会话
3. **IMAP OAuth Token 短期缓存** — 避免重复刷新 access_token
4. **通道缓存** — 记忆账号可用渠道，跳过已知不可用的
5. **IMAP 批量 FETCH** — 多封邮件一次网络往返
6. **IMAP 并发双服务器** — 竞速两台 IMAP 服务器

---

## 2. 功能边界

### 2.1 本期包含

1. Outlook OAuth 账号（`account_type == "outlook"`）的验证码提取路径统一
2. 上述 6 项性能优化
3. 优化同时覆盖 Web 端和外部 API 两个入口
4. 邮件列表获取（`api_get_emails`）同步受益于 #1 权限预检、#5 批量 FETCH、#6 并发双服务器

### 2.2 本期不包含

1. IMAP generic 账号（`account_type == "imap"`）的优化 — 保持现有独立路径
2. SSE 流式渲染 — P1，另开 FD
3. 邮件列表缓存 — P2，另开 FD
4. 邮件详情查看（`api_get_email_detail`）的优化 — 非核心场景
5. 发送/删除邮件的优化

---

## 3. 系统行为设计

### 3.1 路径统一 — 验证码提取统一流程

#### 3.1.1 统一入口函数

新增 service 层统一函数（位于 `services/verification_channel_routing.py` 或独立文件）：

```
extract_verification_for_outlook(
    account: Dict,
    proxy_url: str,
    resolved_policy: Dict,
    code_source: str,
    expected_field: str | None = None,
    from_contains: str = "",
    subject_contains: str = "",
    since_minutes: int | None = None,
    baseline_timestamp: int | None = None,
) -> Dict[str, Any]
```

**返回值**：
```python
{
    "success": bool,
    "data": { ... },           # 提取结果（验证码/链接/formatted）
    "error": { ... },          # 错误信息（当 success=False）
    "channel_used": str,       # 命中的渠道
    "new_refresh_token": str,  # 如有轮换
}
```

#### 3.1.2 统一流程

```
1. build_verification_channel_plan(preferred_channel)
   → 生成有序 channel_plan: [preferred, ...其余]

2. for channel in channel_plan:
   2a. 通道缓存命中检查 → 已知不可用则 skip
   2b. Graph 权限预检 → 无 Mail.Read 则 skip graph_* 渠道
   2c. fetch_emails_for_channel(channel) → success?
       - 失败 → 记录 upstream_error, continue
       - 成功但空 → continue
   2d. (外部 API 专有) filter_messages(from/subject/since)
   2e. fetch_email_detail_for_channel(channel, message_id)
       ※ 当 channel 为 IMAP 时，使用连接复用版本
   2f. extract_verification_info_with_options()
   2g. enhance_verification_with_ai_fallback()
   2h. apply_confidence_gate(enforce_mutual_exclusion=False)
   2i. 判断是否命中 → 命中则更新 preferred_channel, 写通道缓存, return

3. 全部渠道遍历完毕 → 判断 auth_expired / email_not_found
```

> **⚠️ 行为变更说明（路径统一后）**：
> - **apply_confidence_gate**：Web 端当前未调用此函数，统一后将调用 → Web 端低置信度结果可能被过滤
> - **enforce_mutual_exclusion**：Web 端当前使用默认值 `True`（code 和 link 互斥），统一后使用 `False`（code 和 link 可共存）→ Web 端验证码提取可能同时返回 code 和 link
> - 以上行为对齐 External API 的现有行为，对注册场景更合理（同时获取验证码和验证链接）

#### 3.1.3 Web 端调用方式

`controllers/emails.py` `api_extract_verification`:

```python
# account_type == "imap" → 保持现有 imap_generic 路径
# account_type == "outlook" → 调用统一函数
result = extract_verification_for_outlook(
    account=account,
    proxy_url=proxy_url,
    resolved_policy=verification_policy,
    code_source=code_source,
)
# controller 只负责 HTTP 响应封装 + account_summary 更新
```

**预期代码变化**：controller 中 Outlook 路径从 ~600 行缩减至 ~50 行。

#### 3.1.4 外部 API 调用方式

`services/external_api.py` `_extract_verification_with_memory_for_outlook`:

```python
result = extract_verification_for_outlook(
    account=account,
    proxy_url=proxy_url,
    resolved_policy=resolved_policy,
    code_source=code_source,
    expected_field=expected_field,
    from_contains=from_contains,
    subject_contains=subject_contains,
    since_minutes=since_minutes,
    baseline_timestamp=baseline_timestamp,
)
```

**预期代码变化**：外部 API 的 ~130 行循环逻辑迁入统一函数，原函数变为薄包装。

---

### 3.2 Graph API 权限预检

#### 3.2.1 行为

1. `get_access_token_graph_result()` 返回值新增 `scope` 字段（从 token response 解析）
2. 新增工具函数 `has_mail_read_permission(scope: str) -> bool`
3. `get_emails_graph()` 在获取 token 后检查 scope：
   - 无 `Mail.Read` / `Mail.ReadWrite` → 直接返回 `{"success": False, "auth_expired": True, "no_mail_permission": True}`
   - 不发起 Graph messages API 请求
4. 通道缓存可选择记住 "该账号无 Graph 权限"

#### 3.2.2 scope 解析规则

MS token endpoint 返回的 `scope` 是空格分隔的字符串，例如：
```
"User.Read profile openid email"
```
（无 `Mail.Read` = 无邮件读取权限）

检测条件：`"Mail.Read" in scope or "Mail.ReadWrite" in scope`

---

### 3.3 IMAP 连接复用（fetch + detail 共用会话）

#### 3.3.1 行为

新增组合函数（`services/imap.py`）：

```
fetch_and_detail_imap_with_server(
    account: str,
    client_id: str,
    refresh_token: str,
    folder: str = "inbox",
    top: int = 1,
    server: str = IMAP_SERVER_NEW,
) -> Dict[str, Any]
```

**返回值**：
```python
{
    "success": bool,
    "emails": [...],         # 邮件摘要列表
    "detail": { ... },       # 最新一封的完整详情（body/body_html/raw_content）
    "error": { ... },        # 错误信息
}
```

#### 3.3.2 内部流程

```
1. get_access_token_imap_result() → access_token (※ 走缓存)
2. IMAP4_SSL(server) → authenticate(XOAUTH2)
3. SELECT folder
4. SEARCH ALL → message_ids
5. 取最新 top 封的 msg_id
6. FETCH msg_id (RFC822) → 解析为 headers + body_preview → emails[]
7. FETCH 最新 1 封的完整 RFC822 → 解析为 detail（含 body/body_html/raw_content）
8. logout()
```

步骤 6-7 在同一个 IMAP 连接内完成。如果 top=1，步骤 6 和 7 实际上是同一次 FETCH（只需解析出摘要和详情两种视图）。

#### 3.3.3 在 routing 层的集成

`verification_channel_routing.py` 新增：

```
fetch_emails_and_detail_for_channel(
    account, channel, proxy_url, skip, top
) -> Dict
```

当 channel 为 IMAP 时调用 `fetch_and_detail_imap_with_server()`，一次调用同时返回 emails + detail。

当 channel 为 Graph 时，仍分两步（Graph API 的 list 和 detail 是不同的 endpoint，共享 access_token 但无连接复用收益）。

---

### 3.4 IMAP OAuth Token 短期缓存

#### 3.4.1 行为

在 `services/imap.py` 模块级维护 token 缓存：

```python
_token_cache: Dict[str, Tuple[str, float]] = {}  # key → (access_token, expires_at)
_token_cache_lock = threading.Lock()
```

- **key**: `f"{client_id}:{hashlib.sha256(refresh_token.encode()).hexdigest()[:16]}"`
- **TTL**: `expires_in - 60` 秒（MS token 通常 `expires_in=3599`，即缓存 ~59 分钟）
- **命中**: 直接返回缓存的 access_token
- **未命中/过期**: 调用 MS token endpoint，成功后写入缓存

#### 3.4.2 失效场景

- refresh_token 被轮换 → key 自动变化（hash 不同），旧缓存自然过期
- 手动清除：`clear_imap_token_cache(client_id=None)` 清单个或全部

---

### 3.5 通道缓存（Channel Cache）

#### 3.5.1 行为

新建 `services/channel_capability_cache.py`：

```python
_cache: Dict[str, Dict[str, Any]] = {}  # email → { "available": ["imap_new", ...], "unavailable": ["graph_inbox", ...], "expires_at": float }
_cache_lock = threading.Lock()
```

- **写入时机**: `fetch_emails_for_channel()` 返回后，记录该渠道成功/失败
- **读取时机**: channel_plan 遍历前，过滤掉已知不可用的渠道
- **TTL**: 1 小时
- **失效触发**: token 刷新成功 / 账号凭据变更

#### 3.5.2 与 preferred_channel 的职责分工

| 维度 | preferred_channel | 通道缓存 |
|------|-------------------|----------|
| 存储 | DB（持久化） | 进程内存（易失） |
| 语义 | "上次成功的渠道" → 优先尝试 | "已知可用/不可用" → 跳过不可用 |
| 更新时机 | 验证码提取成功时 | 每次渠道调用后 |
| 作用 | 排序 channel_plan | 过滤 channel_plan |

---

### 3.6 IMAP 批量 FETCH

#### 3.6.1 行为

修改 `get_emails_imap_with_server()` 的 FETCH 逻辑：

**现有**：
```python
for msg_id in paged_ids:
    status, msg_data = connection.fetch(msg_id, "(RFC822)")
```

**优化后**：
```python
ids_str = b",".join(paged_ids)
status, all_data = connection.fetch(ids_str, "(RFC822)")
```

一次网络往返获取所有邮件。

#### 3.6.2 兼容性

- 单封（top=1）: `connection.fetch(b"42", "(RFC822)")` → 行为不变
- 多封: `connection.fetch(b"40,41,42", "(RFC822)")` → 一次返回多条
- 返回数据结构: `all_data` 是交错的 `[(header, body), b")", (header, body), b")", ...]`，需按对解析

---

### 3.7 IMAP 并发双服务器

#### 3.7.1 行为

新增 `get_emails_imap_concurrent()` 函数：

```python
def get_emails_imap_concurrent(
    account, client_id, refresh_token, folder, skip, top,
    servers=("outlook.live.com", "outlook.office365.com"),
) -> Dict[str, Any]:
```

使用 `concurrent.futures.ThreadPoolExecutor(max_workers=2)`：
1. 两个线程分别连接两台服务器
2. `as_completed()` 取第一个成功结果
3. 另一个连接 `logout()` 关闭

#### 3.7.2 与通道缓存的交互

- 通道缓存命中（已知 imap_new 可用）→ 只连单服务器，不并发
- 通道缓存未命中 → 并发两台
- 结果写回通道缓存

---

## 4. 数据模型设计

### 4.1 无 DB schema 变更

所有缓存为进程内存，无新增数据库表/字段。

### 4.2 现有字段利用

| 字段 | 表 | 用途 |
|------|-----|------|
| `preferred_verification_channel` | accounts | 渠道记忆（已有） |
| `client_id` | accounts | Token 缓存 key 的一部分 |
| `refresh_token` | accounts | Token 缓存 key 的一部分（加密存储，缓存用 hash） |

---

## 5. 接口契约变更

### 5.1 无外部 API 契约变更

所有优化对外部 API 调用方透明：
- 请求参数不变
- 响应格式不变
- 仅响应时间缩短

### 5.2 内部 service 接口变更

| 函数 | 变更 |
|------|------|
| `get_access_token_graph_result()` | 返回新增 `scope` 字段 |
| `get_access_token_imap_result()` | 增加缓存逻辑（透明） |
| `get_emails_graph()` | 新增 `no_mail_permission` 返回字段 |
| 新增 `fetch_and_detail_imap_with_server()` | 组合函数 |
| 新增 `get_emails_imap_concurrent()` | 并发版本 |
| 新增 `extract_verification_for_outlook()` | 统一入口 |
| 新增 `fetch_emails_and_detail_for_channel()` | routing 层组合 |
| 新增 `channel_capability_cache.py` | 通道缓存模块 |

---

## 6. 关联文件（设计视图）

### 6.1 核心修改

| 文件 | 修改内容 |
|------|----------|
| `outlook_web/services/imap.py` | 连接复用组合函数、Token 缓存、批量 FETCH、并发双服务器 |
| `outlook_web/services/graph.py` | scope 解析、权限预检 |
| `outlook_web/services/verification_channel_routing.py` | 统一入口函数、组合 fetch+detail、通道缓存集成 |
| `outlook_web/controllers/emails.py` | Outlook 验证码路径重构为调用统一函数 |
| `outlook_web/services/external_api.py` | Outlook 验证码路径重构为调用统一函数 |

### 6.2 新建

| 文件 | 用途 |
|------|------|
| `outlook_web/services/channel_capability_cache.py` | 通道能力缓存模块 |

### 6.3 不涉及

| 文件 | 原因 |
|------|------|
| `outlook_web/services/imap_generic.py` | IMAP generic 账号不在本期范围 |
| `outlook_web/db.py` | 无 schema 变更 |
| `templates/` / `static/` | 无前端变更 |
| `outlook_web/routes/` | 路由不变 |

---

## 7. 错误处理

### 7.1 统一函数的错误返回

| 场景 | 错误码 | HTTP 状态 |
|------|--------|-----------|
| 所有渠道都失败 + Graph auth_expired + IMAP 未连通 | `ACCOUNT_AUTH_EXPIRED` | 401 |
| 所有渠道成功但无邮件 | `EMAIL_NOT_FOUND` | 404 |
| 有邮件但提取不到验证信息 | `VERIFICATION_NOT_FOUND` | 404 |
| IMAP 连接成功但信箱为空 | `EMAIL_NOT_FOUND` | 404 |

### 7.2 降级策略

- Token 缓存过期 → 自动刷新，透明降级
- 通道缓存不可用 → 退化为完整 channel_plan 遍历（无缓存等价于当前行为）
- 并发双服务器两个都失败 → 返回最后一个错误

---

## 8. 验收映射（对应 PRD v1.3）

### 8.1 路径统一

1. Web 端 `api_extract_verification` 的 Outlook 路径调用统一函数
2. 外部 API `_extract_verification_with_memory_for_outlook` 调用同一统一函数
3. 两个入口的提取结果行为一致

### 8.2 性能优化

4. 有 preferred_channel 且 IMAP 路径：端到端 ≤ 4s（P90）
5. 无 preferred_channel 冷启动：端到端 ≤ 5s（P90）
6. 无 Graph 权限账号不发起 Graph API 调用（通过 PERF 日志验证）
7. IMAP 连接复用：一次验证码提取只建立 1 次 IMAP 连接（通过 PERF 日志验证）
8. Token 缓存命中率 > 90%（热路径，通过日志统计）

### 8.3 回归

9. IMAP generic 账号验证码提取功能不受影响
10. 外部 API 的 filter 参数（from_contains/subject_contains/since_minutes）仍然生效
11. refresh_token 轮换仍然正确保存
12. 全量测试通过

---

## 9. 实测状态回填（2026-04-12）

> 说明：以下为本次会话真实运行数据，用于修正文档中的“目标值 vs 当前值”认知；
> 不替代正式压测报告。

### 9.1 测试口径

1. 启动口径：
   - 代码：`dev-5.3Codex`
   - 配置来源：`E:\hushaokang\Data-code\outlookEmail\.env`
   - 数据库：`E:\hushaokang\Data-code\outlookEmail\data\outlook_accounts.db`
   - 端口：`5000`
   - 环境：`PERF_LOGGING=true`，`SCHEDULER_AUTOSTART=false`
2. 对照口径：同数据库、同账号、同接口，分别对 `main` 与 `dev-5.3Codex` 各跑 6 次。
3. 指标来源：客户端端到端计时（ms），接口为
   `GET /api/emails/{email}/extract-verification`。

### 9.2 A/B 结果

| 分支 | 样本 | 平均耗时 | 最小值 | 最大值 |
|------|------|----------|--------|--------|
| main | 6 | 6910ms | 5141ms | 9208ms |
| dev-5.3Codex | 6 | 5309ms | 4220ms | 8109ms |

整体平均耗时下降约 **23.2%**（6910ms → 5309ms）。

### 9.3 当前结论（对验收项 4/5）

1. 优化策略已体现收益（尤其在慢路径和均值上）。
2. 但当前样本仍未稳定达到 `≤4s` 目标：
   - 最好值约 4.22s，
   - 仍存在 8s+ 长尾。
3. 因此本期状态应标注为：**“有效但未完全达标，需继续优化尾延迟”**。

### 9.4 已批准的下一步增量（会话决议）

1. 本会话已确认优先落地“Top-N 固定下调”策略：
   - 验证码提取统一链路将拉取窗口从 `top=20` 调整为 `top=3`。
2. 本次决议不引入配置项，以最小改动验证收益。
3. 若后续出现漏检风险，再评估是否升级为可配置方案。

### 9.5 固定 `top=3` 实测结果（2026-04-12 当日回填）

1. 变更内容：统一提取链路拉取窗口由 `top=20` 调整为固定 `top=3`。
2. 结果：在当前真实账号样本下出现明显副作用：
   - 多次返回 `404`（`VERIFICATION_NOT_FOUND`），
   - 同时整体平均耗时上升（存在 15s~20s 长尾）。
3. 结论：
   - 该策略在当前数据分布下不满足“低风险提速”预期，
   - 不建议直接作为默认策略上线。

### 9.6 `dev` 分支合并后测试回填（2026-04-12）

1. 按“后台独立进程”方式完成回归测试：
   - `pytest tests/test_verification_channel_memory_v1.py tests/test_web_graph_auth_fallback.py tests/test_extract_verification_group_policy.py tests/test_external_api.py tests/test_channel_capability_cache.py -q`
   - 结果：`139 passed in 138.80s`。
2. 按“后台独立进程”方式完成全量测试：
   - `pytest -q`
   - 结果：`1018 passed, 9 skipped in 481.62s`。
3. 本轮性能实测未形成新的有效样本统计（仅保留此前 A/B 结论），原因是运行控制被多次后台服务启停打断。
4. 现场机制说明（避免“进程停不下来”误解）：
   - `Start-Process` 启动的是常驻后台服务进程，默认不会自行退出；
   - 命令执行超时只会结束当前命令等待，不会自动结束已启动的后台 PID；
   - 需显式停止对应 PID/端口监听进程后才算完整回收现场。

### 9.7 按用户指定顺序的三项验证（2026-04-12）

按“性能 → 全量 → 人工验收”顺序执行，均在 `dev` 分支完成：

1. 性能采样（2 账号 × 3 次）：
   - 结果：`avg=6137.83ms`、`min=4368.63ms`、`max=8375.27ms`；
   - 有效样本 6/6，HTTP 200 全通过。
2. 全量测试：
   - `pytest -q` → `1018 passed, 9 skipped in 386.35s`。
3. 人工验收脚本：
   - `tests/verify_verification_ai_endpoints.py` 成功跑通 6/6 步骤；
   - 登录/设置/CSRF/AI 探测/账号读取/提取接口均返回成功；
   - 本次自动选中账号 `zerodotsix@126.com`，提取接口 `status=200`、`success=true`。

### 9.8 会话续跑：清理临时脚本并为 AI 配置留出运行环境（2026-04-12）

1. 已按用户选择先清理临时测试脚本（`tmp_benchmark_extract.py`、`tmp_run_server.py`）。
2. 应用户要求“启动后我来配置 AI 模型功能”，已仅启动 1 个后台服务实例供用户操作。
3. 当前服务监听：`127.0.0.1:5000`（后台进程，便于页面配置与联调）。
4. 本条仅记录运行态准备动作，不新增性能结论。

### 9.9 AI 配置后性能复测（2026-04-12）

1. 用户完成 AI 模型相关配置后，按同口径执行 6 次性能样本（2 账号 × 3 次）。
2. 结果：`avg=5384.73ms`、`min=3795.02ms`、`max=8278.73ms`，全部 `status=200`。
3. 与配置前同会话采样（`avg=6137.83ms`）相比：
   - 平均耗时下降约 **12.3%**（降低约 753ms）。
4. 当前结论：
   - AI 配置完成后本轮样本体感有改善，且最佳值进入 4s 内；
   - 但长尾（8s+）仍存在，尚需更多样本验证稳定性。

### 9.10 AI 配置后全量稳定性复测（2026-04-12）

1. 在 AI 配置后立即执行全量测试：`pytest -q`。
2. 结果：`1018 passed, 9 skipped in 395.42s (0:06:35)`，无新增失败。
3. 结论：本轮配置调整未引入可见回归，当前分支可继续进入后续验收步骤。

### 9.11 AI 配置后人工验收复跑（2026-04-12）

1. 使用脚本 `tests/verify_verification_ai_endpoints.py` 复跑人工验收链路。
2. 结果：6/6 步骤全部通过，且 `stderr` 为空。
3. 验证点摘录：
   - 登录成功；
   - AI 探测返回 `connectivity_ok=true`、`contract_ok=true`；
   - 提取接口返回 `status=200`、`success=true`。
4. 现状提示：当前 settings 显示 `verification_ai_enabled=false`，但 AI 探测接口在配置后已可连通并通过契约检查。

---

**文档结束**
